const prefixPath = "";
const prefixPathAdmin = "/admin";

export const ROUTES = {
  HOME: `${prefixPath}`,
  AUTH: `${prefixPath}/auth`,
};

export const ROUTES_ADMIN = {
  HOME: `${prefixPathAdmin}`,
};
